## Code Quality
* Set the ASK_API_URL environment variable to the base URL of the API.
* Define the ASK_MODEL environment variable with the model name.
* Configure the ASK_API_KEY environment variable with a valid API key.
* Verify the environment variables are correctly set before making API requests.
* Validate API credentials and URLs to ensure successful connection and data exchange.

## Performance
* Set ASK_API_URL environment variable
* Set ASK_MODEL environment variable
* Set ASK_API_KEY environment variable
* Verify API endpoint and credentials
* Validate environment variable configuration

## Security
* Set ASK_API_URL environment variable
* Set ASK_MODEL environment variable
* Set ASK_API_KEY environment variable
* Verify environment variables are correctly configured
* Test API connection to ensure successful setup
